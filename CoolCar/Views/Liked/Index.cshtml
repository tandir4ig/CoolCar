@model List<Car>

@if (Model.Count == 0)
{
    <div style="padding-top: 200px; text-align:center">
        <h2 class="text-center">У вас нет избранных товаров</h2>
        <br /><br />
        <a class="btn btn-dark" style="font-size:20px" title="Сравнить" asp-controller="home" asp-action="catalog">Перейти в каталог</a>
    </div>

}
else
{
    <div class="row">
        <h2>Избранное</h2>
        <div class="col">
            <table class="table">
                <thead>
                    <tr>
                        <th>Название</th>
                        <th>Цена</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var car in Model)
                    {
                        <tr>
                            <td>@car.Name</td>
                            <td>@car.Cost</td>
                            <td class="col-3 text-end">
                                <a class="btn btn-dark bi bi-clipboard2-plus" title="Сравнить" asp-controller="liked" asp-action="remove" asp-route-carid="@car.Id"></a>
                                <a class="btn btn-dark bi bi-cart4" title="Добавить в корзину" asp-area="" asp-controller="Card" asp-action="Add" asp-route-carid="@car.Id"></a>
                                <a class="btn btn-dark bi bi-trash-fill" title="Удалить из избранного" asp-controller="liked" asp-action="remove" asp-route-carid="@car.Id"></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <div class="text-center">
            <a class="btn btn-dark" title="Сравнить" asp-controller="home" asp-action="catalog">Продолжить покупки</a>
            <a class="btn btn-dark" title="Сравнить" asp-controller="liked" asp-action="clear">Очистить список</a>
        </div>
    </div>
}






