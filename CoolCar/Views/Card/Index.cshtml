@model Card 

@if(Model == null)
{
    <h3>Корзина пустая</h3>
}
else
{
    <div class="row">
        <div class="col">
            <table class="table">
                <thead>
                    <tr>
                        <th>Название</th>
                        <th>Цена</th>
                        <th>Количество</th>
                        <th>Стоимость</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Cars)
                    {
                        <tr>
                            <td>@item.car.Name</td>
                            <td>@item.car.Cost</td>
                            <td>@item.Amount</td>
                            <td>@item.Cost</td>
                        </tr>
                    }
                </tbody>
            </table>
            <div class="text-right">
                <h3>Итого:</h3>
                <h3>@Model.TotalSum</h3>
            </div>
            <div class="text-center">
                <a class="btn btn-dark" href="/Home/Catalog" role="button">Продолжить покупки</a>
                <a class="btn btn-dark"  href="#" role="button">Оформить заказ</a>
            </div>
        </div>
    </div>
}